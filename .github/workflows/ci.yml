name: CI

on:
  push:
    branches:
      - main
      - feature/abridge-anthropic-ehr-stack
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --no-audit --no-fund

      - name: Build and regenerate route tree
        run: npm run build

      - name: Typecheck generated application
        shell: bash
        run: |
          set -o pipefail
          npx tsc --noEmit 2>&1 | tee tsc.log

      - name: Audit legacy hackathon stack references
        if: always()
        shell: bash
        run: |
          grep -R -n -E 'Butterbase|butterbase|Neo4j|neo4j|RocketRide|rocketride|Daytona|daytona|Cognee|cognee' src package.json README.md > legacy-stack.log || true
          cat legacy-stack.log

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-diagnostics
          path: |
            tsc.log
            legacy-stack.log
          if-no-files-found: error
